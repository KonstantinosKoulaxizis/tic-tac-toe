/**
 * Player's one mark
 */
export const X_MARK = 'X'

/**
 * Player's two mark
 */
export const O_MARK = 'O'

/**
 * Array containing players marks
 */
export const MARKS: [typeof X_MARK, typeof O_MARK] = [X_MARK, O_MARK]

/**
 * Initial grid's value used
 */
export const INITIAL_GRID_NUMBER: number = 3

/**
 * Available grid values
 */
export const GRID_VALUES: number[] = [3, 4, 5]
